import { StyleSheet } from "react-native";

const commonStyles = StyleSheet.create({
  container: {
    flex: 1
  },
  rowSpaceBetween2: {
    flexDirection: "row", 
    justifyContent: "space-between"
  }
})

export {commonStyles};
